# Solana project in progess - Themed GIF uploading site

To get started, clone this repo and follow these commands:

Run `npm install` at the root of your directory
Run `npm run start` to start the project
